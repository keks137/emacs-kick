;; Set the initial frame parameters to avoid a flash of a light background.
(setq default-frame-alist
'((background-color . "#1e1e1e")   ;; dark background color
(foreground-color . "#000000")))   ;; light text
